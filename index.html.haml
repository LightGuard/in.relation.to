---
layout: base
---

.row
  .span9
    %h1 In relation to blog
    .lead
      The Hibernate team blog
    - for post in page.posts
      .blogpost
        %h2
          %a{:href=>post.url}= post.title
        .blogtagbar
          #{post.author}
          &nbsp;|&nbsp;
          #{post.date.strftime( '%Y-%m-%d' )}
          &nbsp;|&nbsp;
          - post.tags.each do |tag|
            %a.label{:href=>tag.primary_page.url}
              = tag
            &nbsp;
        %p
          ~ post.content
        %hr
    = page.posts.links
  .span3
    %h2
      Tags
    = site.posts_tag_cloud
